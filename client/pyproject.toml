[project]
name = "llm-deployment-stack-client"
version = "0.1.0"
description = "LangChain + MLflow test client for the LLMOps stack"
requires-python = ">=3.11"
dependencies = [
    "langchain-openai>=0.2.0",
    "langchain-core>=0.3.0",
    "mlflow>=2.17.0",
    "python-dotenv>=1.0.0",
]
